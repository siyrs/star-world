# 问题记录

| 问题 ID | 优先级 | 地图/模块 | 状态 | 复现步骤 | 复现证据 | 根因 | 修改文件 | 修复方案 | 自动化回归 | 独立 QA 结果 |
|---|---|---|---|---|---|---|---|---|---|---|
| BUG-REL-001 | P1 | Windows 发布自动化 | qa-passed | 在 Windows PowerShell 5.1 执行发布 smoke | 原 `ArgumentList` 在 PS5.1 不可用致导出前失败 | 同上 | `tests/release/run_windows_export_smoke.ps1` | 加 PS7+ 前置检查，BUILD.md 统一到 `pwsh` | fresh export smoke 16/16 | 本轮验证通过（2026-08-02） |
| BUG-UI-001 | P2 | UI 主题/Windows 导出 | qa-passed | 用 PowerShell 7 运行 fresh release smoke | 原 stderr 报像素字体缺失回退 | 动态加载字体未被 export 资源依赖收集 | `export_presets.cfg` | 字体纳入导出包 | fresh export stderr 无 font-fallback warning | 本轮验证通过（2026-08-02） |
| BUG-PERF-001 | P1 | 初始世界流式加载 | qa-passed | fresh release smoke 运行 soak 并读取 JSON 终态 | 原 pending=62 超 warning=48；且缺跨区域性能数据 | chunk 健康需在沉降后才告警；缺性能基线 | `src/diagnostics`; `tests/qa/performance_scenario_capture.gd` | chunk 健康要求 settlement 后才 flag；补齐跨五图场景、外部内存和 streaming 终态采集 | 13 场景报告、外部内存和 runtime soak | 流式健康门禁通过；目标硬件仍由 BUG-PERF-002 跟踪 |
| BUG-PERF-METRIC-001 | P1 | 性能测量定义 | qa-passed | 对旧滚动 FPS 取 1 分位并与同批逐帧耗时比较 | 旧主菜单曾出现 `1% Low=1 FPS`，但 p95 帧时约 6ms | `Engine.get_frames_per_second()` 不是单帧 FPS，不能用于逐帧 1% Low | `src/core/frame_performance_metrics.gd`; `tests/qa/frame_performance_metrics_regression.gd`; `qa/performance-baseline.md` | 平均 FPS、1% Low、p95/p99 和预算统一从真实 frame time 计算；滚动 FPS 仅保留 diagnostic | 确定性慢帧夹具 23 checks；五图 schema v2 数据一致 | run `30772768777` 全绿（2026-08-03） |
| BUG-PERF-COLUMN-001 | P1 | 世界生成器/天空群岛 | qa-passed | 同一 16×16×64 天空群岛 Chunk 分别关闭/开启缓存并统计真实计算 | 缓存关闭：地表 16,424 次、岛屿 17,418 次 | 生成器未缓存 Profile+Seed 下不可变的柱级结果 | `src/world/world_generator_column_cache.gd`; `src/world/world_generator.gd`; `src/world/voxel_world.gd` | 容量 8,192、硬上限 32,768 的确定性 FIFO 缓存；Profile/Seed 失效；遥测 | 345,428→116,287μs；16,384 方块差异 0；五 Profile 输出一致 | 专项、全量 Godot、桌面 UI、Windows 导出均通过（2026-08-03） |
| BUG-INV-CANONICAL-001 | P2 | 背包保存结构 | qa-passed | 无 metadata 普通堆叠执行 `serialize→deserialize→serialize` | 旧反序列化会补入空 metadata，导致结构漂移 | 未区分缺失、空字典、有效字典和错误类型 | `src/inventory/inventory_service.gd`; `tests/qa/inventory_canonical_roundtrip_regression.gd` | 仅保留非空合法 metadata；旧空值归一化；错误类型忽略；相邻有效 metadata 保留 | canonical 回归由 13 扩展到 21 checks | run `30780260473` 与 `30780260456` 通过（2026-08-03） |
| BUG-INV-METADATA-TYPE-001 | P1 | 背包/存档 metadata | qa-passed | 修理后保存世界，读取 `world.json` 并与保存前背包做精确 Dictionary 比较，再完整重载 | 首轮真实修理旅程 53 checks 中仅“world.json 精确背包”和“重载精确背包”失败；耐久、名称、材料数量分别都正确 | JSON 数值边界会把 `durability`、`magazine_rounds` 等整数 metadata 读成浮点形态；旧 canonical 规则只处理空字段，没有规范已知数值类型 | `src/inventory/inventory_service.gd`; `tests/qa/inventory_canonical_roundtrip_regression.gd`; 两个领域 validator | 建立集中 `_canonicalize_metadata()`；保存和反序列化统一把已知整数键恢复为 int，非法已知值剔除，未知 metadata 原样保留；合成合同改验证行为策略而非旧代码文本 | 真实 JSON stringify/parse、浮点旧值、非法已知值、空/畸形/相邻 metadata，共 21 checks；合成 78 和修理 53 完整重载均精确相等 | release `30780260450`、craft `30780260456`、repair `30780260473`、full `30780260331` 全绿（2026-08-03） |
| BUG-QA-CONTENT-CRAFT-001 | P1 | 合成玩家闭环 | qa-passed | 旧证据主要为配方/服务合同，无法证明正式 UI、失败和跨会话重载 | 旧矩阵把 E2 当覆盖；按钮无稳定身份；无真实 C/右键/鼠标/完整重载 | UI 可观察性和 E3 旅程缺口 | `src/ui/crafting_panel.gd`; `tests/qa/crafting_desktop_acceptance.gd`; `qa/content-journey-matrix.md`; 专项 workflow/validator | 稳定 `recipe_id/output_item_id/station`；成功/失败反馈；真实键鼠；两类失败零副作用；权威保存和完整重载 | 合成桌面 78；canonical 21；事务 27；integration 34；input 33；block 34；五图 57；相邻四旅程通过 | run `30780260456` 全绿；整体 BUG-QA-CONTENT-001 仍 open（2026-08-03） |
| BUG-QA-CONTENT-REPAIR-001 | P1 | 修理玩家闭环 | qa-passed | 旧修理桌面脚本只组装测试服务，未进入真实世界、修理台方块、权威保存和完整菜单重载 | 无法证明玩家在生产世界完成修理，也不能证明失败原子性、可见反馈和 metadata/世界覆盖跨会话恢复 | 合成式 fixture 替代生产 composition；按钮缺稳定目标身份；UI 同步处理与服务 signal 重复；没有 JSON 精确重载门禁 | `src/ui/repair_panel.gd`; `tests/qa/repair_desktop_acceptance.gd`; `.github/workflows/player-repair-closed-loop-tests.yml`; `tests/developer_b/validate_repair_closed_loop.ps1`; QA 矩阵 | 真实 `game.tscn`/体素修理台/中心射线/右键/鼠标；稳定 `target_id/item_id/target_kind`；服务 signal 单一反馈源；无材料禁用；只读运行时端口制造已启用失败并验证零修改；保存、菜单返回和完整重载 | repair domain 32；canonical 21；事务 27；input 33；block 34；五图 57；生产桌面 53；截图为 1024×576 | run `30780260473` 全绿；截图显示 113/251 耐久、剩余铁锭和“背包服务不可用”失败反馈；full `30780260331` 全绿（2026-08-03） |
| BUG-QA-CONTENT-001 | P1 | 全部正式内容闭环 | open | 对照 `qa/content-journey-matrix.md` 逐领域检查正式输入、成功、失败、保存和完整重载 | 合成与修理已经达到 E3 闭环；其他领域证据等级不一致，部分只有 E2 或 E3+保存文件检查 | 历史测试按服务合同增长，未形成统一的玩家闭环完成定义 | `qa/content-journey-matrix.md`; 各领域 desktop/regression/workflow | 按领域逐项补齐真实输入、失败、权威保存、菜单返回和完整重载；不得用一个子领域完成代表全内容完成 | 当前 permanent gate 已覆盖合成、修理及多个相邻旅程 | **仍阻塞发布**：优先补机器跨会话、畜牧/牧场活体计时、农业全生产链、床完整回床、探索里程碑（2026-08-03） |
| BUG-LEAK-001 | P1 | 测试出口资源泄漏 | qa-passed | 全量日志原报 ObjectDB 29、Resource 8 | 裸 Node 未释放；audio_service 未 dispose | 测试生命周期清理不足 | 相关测试与 audio cleanup | free 裸 Node、dispose audio、延长释放等待 | 4 套件逐一 0 泄漏 | 全量回归通过（2026-08-03） |
| BUG-SPAWN-001 | P1 | 星辰大陆出生体验 | qa-passed | 固定 `star_continent/24681357` 复现树冠出生 | first-fit 单列评分不足 | 生成器出生评分不足 | `data/spawn_quality_profiles.json`; `src/world/spawn_quality_registry.gd`; `src/world/world_generator.gd` | 数据驱动有界评分、树冠夹具、碰撞/接缝/坡度门禁 | 5×6 seeds 279 checks + 专项 18/18 | 五图正常入口与出生均通过 |
| BUG-OBS-001 | P2 | 运行时性能遥测 | qa-passed | 检查 Windows 导出 JSON 的 `memory_mib` | `MEMORY_STATIC` 返回 0 | 平台 API 不可用 | runtime telemetry 与外部采样 | 0 改为 -1 unavailable；外部进程采样补证 | fresh smoke + memory JSON | 验证通过（2026-08-02） |
| BUG-QA-002 | P1 | Desktop acceptance runner | qa-passed | desktop runner 要求精确主输出 | 内层通过但主截图路径缺失导致外层失败 | 脚本只写命名截图 | `tests/qa/ui_visual_refresh_desktop_acceptance.gd` | 同时写 primary 与命名 captures | 32 checks / 10 captures | QA-002 独立验证通过 |
| BUG-UI-002 | P1 | 地图选择/设置 UI | qa-passed | 真实 hover/pressed/focus 对比度复测 | 多个状态低于 4.5 | 首轮只覆盖局部 token 组合 | design tokens/theme/textures 与 UI 回归 | 修正所有有效 variation×state 至 ≥4.5 | design-system 137、visual 32、accessibility 28、layout 24、journey 58 | QA-003 通过，无 fatal/error/leak |
| BUG-VERSION-001 | P1 | 发布版本身份 | qa-passed | 查看项目、Windows 文件和菜单版本 | 原显示 1.1.0 | 元数据未同步 | `project.godot`; `export_presets.cfg` | 统一 1.3.0 / 1.3.0.0 | fresh EXE 属性一致 | 验证通过（2026-08-02） |
| BUG-PACK-001 | P1 | Windows 导出内容 | qa-passed | 检查 PCK 导出日志 | 原包含 `build/*` QA 资源 | preset all_resources 未排除 | `export_presets.cfg` | 排除 docs/tests/.godot/build/qa | fresh export 不含 build QA 资源 | smoke 16/16 |
| BUG-LAVA-001 | P2 | 深渊世界熔岩 | qa-passed | 玩家进入固定熔岩柱 | 原可游泳且 5 秒伤害为 0 | water/lava 共用 fluid 判断，无伤害系统 | 玩家流体/熔岩逻辑 | 独立 lava 检测与 4.0/0.5s 伤害门控 | lifecycle 32 checks | QA-003 复测通过 |

## 状态说明

- `open`：已复现，待定位或修复。
- `fixing`：Developer 正在修复。
- `self-tested`：Developer 自测通过，待独立 QA。
- `qa-passed`：独立 QA 重测通过。
- `blocked`：经过实际尝试仍受外部条件阻塞，必须附证据。

## 外部测试工具边界

- `BLOCKER-GUI-001`：Computer Use 能启动并唯一识别 fresh EXE 窗口，但激活/捕获连续失败：`failed to activate captured window`，刷新后为 `GetCursorPos failed: 拒绝访问 (0x80070005)`。已停止盲输入；改用 production-scene desktop InputEvent 自动化。该阻塞不等同于游戏缺陷，也不把 E3 production-scene 证据冒充 export-EXE E4 人工输入。
- `BLOCKER-PERF-CI-001`：GitHub Windows Runner 的 OpenGL 3.3 不可用，Godot 自动切换到 ANGLE + `Microsoft Basic Render Driver`；该 Job 只验证 schema、数据一致性、缓存遥测、外部内存链路和无崩溃，不能作为最低/推荐玩家硬件 FPS。