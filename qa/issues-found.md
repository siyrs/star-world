# 问题记录

| 问题 ID | 优先级 | 地图/模块 | 状态 | 复现步骤 | 复现证据 | 根因 | 修改文件 | 修复方案 | 自动化回归 | 独立 QA 结果 |
|---|---|---|---|---|---|---|---|---|---|---|
| BUG-REL-001 | P1 | Windows 发布自动化 | qa-passed | 在 Windows PowerShell 5.1 执行发布 smoke | 原 `ArgumentList` 在 PS5.1 不可用致导出前失败 | 同上 | `tests/release/run_windows_export_smoke.ps1` | 加 PS7+ 前置检查（脚本头 9-18 行），BUILD.md 统一到 `pwsh`；本轮 fresh export 经 pwsh 跑通 16/16 | fresh export smoke 16/16（`build/claude-final-export`） | 本轮验证通过（2026-08-02） |
| BUG-UI-001 | P2 | UI 主题/Windows 导出 | qa-passed | 用 PowerShell 7 运行 fresh release smoke | 原 stderr 报像素字体缺失回退 | 动态加载字体未被 export 资源依赖收集 | `export_presets.cfg` 资源边界 | 字体纳入导出包；本轮 fresh export `release-smoke.stderr.log` 为空，无 font-fallback warning | fresh export stderr 0 行 font warning（`build/claude-final-export`） | 本轮验证通过（2026-08-02） |
| BUG-PERF-001 | P1 | 初始世界流式加载 | qa-passed | fresh release smoke 运行 180 soak 帧并读取 JSON 终态 | 原 pending=62 超 warning=48；且缺跨区域性能数据 | chunk 健康需在沉降后才告警；缺性能基线 | `src/diagnostics`(commit 4823a3c)；`tests/qa/performance_scenario_capture.gd` | chunk 健康要求 settlement 后才 flag；补齐跨五图场景、外部内存和 streaming 终态采集 | 13 场景报告、外部内存和 runtime soak；旧 FPS 列后续由 BUG-PERF-METRIC-001 纠偏 | 流式健康门禁通过；FPS 发布结论以 schema v2 为准 |
| BUG-PERF-METRIC-001 | P1 | 性能测量定义 | qa-passed | 对旧 `performance_scenario_capture.gd` 的 `fps_samples` 取 1 分位并与同批逐帧耗时比较 | 旧主菜单曾出现 `1% Low=1 FPS`，但 p95 帧时约 6ms；两个指标相互矛盾 | `Engine.get_frames_per_second()` 是滚动显示统计，不是单帧 FPS；对它取分位不能代表最慢 1% 真实帧 | `src/core/frame_performance_metrics.gd`; `tests/qa/performance_scenario_capture.gd`; `tests/qa/frame_performance_metrics_regression.gd`; `qa/performance-baseline.md` | 平均 FPS 和 1% Low 统一由真实 frame time 计算；增加 p95/p99、30/60 FPS 预算；滚动 FPS 仅保留 diagnostic；报告升级 schema 2 | 确定性慢帧夹具 23 checks；五图桌面报告逐场景验证 `avg_fps≈1000/frame_ms_avg`、`1% Low≤avg`、预算范围有效 | GitHub Actions run `30772768777` 全绿；指标定义关闭，目标硬件性能仍由 BUG-PERF-002 跟踪（2026-08-03） |
| BUG-PERF-COLUMN-001 | P1 | 世界生成器/天空群岛 | qa-passed | 对同一 16×16×64 天空群岛 Chunk 逐格调用 `get_block()`，分别关闭和开启缓存并统计真实函数执行次数 | 缓存关闭：地表高度 16,424 次、岛屿强度 17,418 次；同一 `(x,z)` 被不同 Y 与邻居查询反复计算 | 生成器未缓存 Profile+Seed 下不可变的柱级地表高度和岛屿强度 | `src/world/world_generator_column_cache.gd`; `src/world/world_generator.gd`; `src/world/voxel_world.gd`; 专项 validator/workflow | 加入容量 8,192、硬上限 32,768 的确定性 FIFO 柱缓存；账本压缩；Profile/Seed 失效；运行时命中/淘汰/计算次数遥测 | 同进程基准：345,428→116,287μs（约 2.97×）；地表 16,424→256；岛屿 17,418→256；16,384 方块完全一致；五 Profile 缓存开关逐项一致 | 专项 49 checks、相邻 streaming/spawn/profile/soak、全量 Godot、桌面 UI、Windows 导出均通过（2026-08-03） |
| BUG-INV-CANONICAL-001 | P2 | 背包保存结构 | qa-passed | 创建无 metadata 的普通堆叠，执行 `serialize→deserialize→serialize` 并比较两个 Dictionary | 原反序列化会给普通堆叠补入 `"metadata": {}`，导致同一背包完整重载后结构发生无意义漂移；旧空 metadata 和畸形 metadata 也没有统一规范化 | `InventoryService.deserialize()` 无条件构造 metadata 字段，没有区分缺失、空字典、有效字典和错误类型 | `src/inventory/inventory_service.gd`; `tests/qa/inventory_canonical_roundtrip_regression.gd` | 先构造 canonical `item_id/count`；仅在 metadata 为非空 Dictionary 时复制；旧空 metadata 自动归一化，错误类型被忽略，其他槽有效 metadata 保留 | canonical roundtrip 13 checks：精确结构相等、选择槽、旧空 metadata、畸形 metadata、相邻有效 metadata 均通过 | 专项 run `30775065134`、主仓库 run `30775065042` 全绿；无数据丢失或泄漏（2026-08-03） |
| BUG-QA-CONTENT-CRAFT-001 | P1 | 合成玩家闭环 | qa-passed | 对照旧内容矩阵和合成测试：旧证据主要是配方列表、事务服务或直接调用，无法证明玩家通过正式 UI 完成随身/工作台合成、失败反馈和跨会话重载 | 旧矩阵将 E2 服务合同作为合成覆盖；按钮无稳定配方身份；面板没有明确服务失败反馈；没有真实 C 键、右键工作台、鼠标按钮、返回菜单和完整重载旅程 | UI 可观察性和 E3 测试缺口；测试若依赖中文按钮文本或不可见滚动坐标会产生假绿/假失败 | `src/ui/crafting_panel.gd`; `tests/qa/crafting_desktop_acceptance.gd`; `qa/content-journey-matrix.md`; 专项 validator/workflow | 配方按钮加入稳定 `recipe_id/output_item_id/station`；成功/失败可见反馈；真实 C 键、真实射线+右键工作台、滚动到可见按钮后真实鼠标点击；禁用和已启用条件变化两种失败均零事务副作用；保存、菜单返回和完整重载精确验证 | 合成桌面 78 checks；domain：canonical 13、事务 27、integration 34、input 33、block 34、五图 profile 57；切石机/畜牧/牧场产物/修理桌面相邻复审全部通过 | 专项 run `30775065134` 与主仓库 run `30775065042` 全绿；截图显示真实工作台面板和失败反馈；整个 `BUG-QA-CONTENT-001` 仍保持 open（2026-08-03） |
| BUG-LEAK-001 | P1 | 测试出口资源泄漏 | qa-passed | 全量日志报 ObjectDB 2+13+8+6=29、Resource 3+3+2=8 | core smoke/tool harvest/agriculture/irrigation 出口泄漏 | ① InventoryService/ToolService extends Node 的裸实例未释放（tool_harvest 5、agriculture 2、irrigation 1）② core_smoke 未调 audio_service.dispose()（ambient 流缓存泄漏 AudioStreamWAV） | commit 8df73c3 | ① 函数末尾 free 裸 Node ② 调 dispose() ③ hub 释放加长等待帧 | 4 测试逐一套件 0 泄漏（ObjectDB/Resource 计数为 0） | 本轮验证通过（2026-08-02）；全量 113/113 验证中 |
| BUG-SPAWN-001 | P1 | 星辰大陆出生体验 | qa-passed | 固定复现 `star_continent/24681357` 树冠出生 | 原 first-fit 单列 headroom+resolver 下调 1m | 生成器评分不足 | `data/spawn_quality_profiles.json`; `src/world/spawn_quality_registry.gd`; `src/world/world_generator.gd`; `tests/qa/spawn_experience_regression.gd` | 数据驱动有界评分（commit 30a0260 起）+ 合成树冠夹具 + 3.7 碰撞/接缝/坡度（2c7370c) | 5×6 seeds 279 checks + star/24681357 专项 18/18 + collision_seam_probe | 本轮 deep journey 五图出生点均 PLAYER SPAWN RESOLVE 正常（2026-08-02） |
| BUG-OBS-001 | P2 | 运行时性能遥测 | qa-passed | 检查 fresh smoke JSON 的 `telemetry.memory_mib` | 原 `0.0` 不满足可观测性 | 导出 Windows 下 `MEMORY_STATIC` 返回 0 | commit f06be2e | MEMORY_STATIC 返回 0 时标记 `-1.0`=unavailable，不谎报 0；外部进程采样补证 | fresh smoke `memory_mib:-1.0`(unavailable，正确）+ `release-smoke.memory.json` 外部样本 | 本轮验证通过（2026-08-02） |
| BUG-QA-002 | P1 | Desktop acceptance runner | qa-passed | 用 `run_godot_desktop_test.ps1` 执行 `ui_visual_refresh_desktop_acceptance.gd`，OutputPath=`...ui-visual-refresh-main.png` | 原始失败：内层 `QA ... PASS checks=32 captures=10`，外层因精确主输出缺失 exit 1。Developer 证据 `build/developer-selftest-qa002`：内层 32/10 且外层 exit 0 | 测试脚本只写命名截图，未同时兑现调用方/runner 要求的精确主截图路径 | `tests/qa/ui_visual_refresh_desktop_acceptance.gd` | 同时写调用方 primary OutputPath 与 10 个命名 captures；未放宽 runner 门禁 | Developer focused desktop exit 0；精确主输出、命名截图、JSON/日志已生成 | QA-002 PASS：`build/qa-independent-qa002-20260731-1057`；exit 0，32/10，primary SHA-256 `F9CCAD6ED9408CB6979490E39CC24E6F3009C6C8E4BA034BDABEBECC252ABCD5`，无 fatal/error/leak |
| BUG-UI-002 | P1 | 地图选择/设置 UI | qa-passed | 运行 UI desktop journey 并查看 1280×720 地图选择与设置截图 | 原始低对比/拥挤已改善；QA-002 仍测得 Button normal/hover/focus 3.843/2.605/3.672，Primary 4.097/2.907/3.914，Card pressed 3.247，Selected pressed 4.333，Ghost hover/pressed 2.352/1.155，均低于 4.5 | 首轮仅修 panel-context Card/Ghost normal 等局部状态；64-check 没覆盖真实交互 variation 的 hover/pressed/focus，实际纹理背景与文字 token 组合仍不合格 | `src/ui/design_tokens.gd`; `src/ui/theme_factory.gd`; `src/ui/pixel_ui_textures.gd`（待 Developer 最小调整）; map/settings 使用点；`tests/qa/ui_design_system_regression.gd` | 保持视觉层级，修 Button/Secondary/Primary/MenuPrimary/Card/Selected/Ghost/Danger 的 normal/hover/pressed/focus 有效组合至 ≥4.5；disabled 单列可辨识性，不通过改阈值伪造 | 扩展 variation×state 自动化；重跑 64-check、1280 32/10、1024 map/settings，并用真实 pointer hover/mouse-down/focus 截图 | QA-003 PASS：`build/qa-independent-qa003-20260802-2007`；5 用例全过（design-system 137 checks 全部 ≥4.5、visual-refresh 32 checks/10 captures、accessibility 28 checks、layout-1024 24 checks、profile-journey 58 checks），无 fatal/error/leak |
| BUG-VERSION-001 | P1 | 发布版本身份 | qa-passed | 查看 `project.godot`、`export_presets.cfg` 和 fresh 菜单截图 | 原项目/Windows 文件/菜单显示 `1.1.0` | 版本元数据未随功能提交同步 | `project.godot`; `export_presets.cfg` | 统一为 1.3.0：project.godot `config/version="1.3.0"`，export_presets `file_version/product_version="1.3.0.0"` | fresh EXE 属性与配置一致（`build/claude-final-export`） | 本轮验证通过（2026-08-02） |
| BUG-PACK-001 | P1 | Windows 导出内容 | qa-passed | 运行 fresh export 后检查 `export.stdout.log` | 原 PCK 含 `res://build/*.png.import` QA 截图 | preset 为 `all_resources` 且未排除 `build/*` | `export_presets.cfg` | `exclude_filter="docs/*,tests/*,.godot/*,build/*,qa/*"`；本轮 fresh export `export.stdout.log` 中 `res://build/` 出现 0 次 | fresh export log 不含 build QA 资源（`build/claude-final-export`），smoke 16/16 | 本轮验证通过（2026-08-02） |
| BUG-LAVA-001 | P2 | 深渊世界熔岩 | qa-passed | 深渊 profile 固定 seed 112358，定位 y==4 洞穴熔岩柱，将玩家放入熔岩 | `tests/qa/water_lava_lifecycle_regression.gd` 运行时证据：`_is_in_fluid()` 把 `lava` 与 `water` 等同；熔岩中可正常游泳；连续 5 秒熔岩接触伤害为 0 | `first_person_player.gd:_is_in_fluid()` 对 `["water","lava"]` 一视同仁；全仓库无熔岩伤害/环境伤害系统 | PM 决策：熔岩应造成伤害（2026-08-02） | `first_person_player.gd` + `ladder_climbing_player.gd` 增加 `_is_in_lava()` + `_apply_lava_contact_damage()`（4.0/0.5s 间隔门控，离开即停） | `water_lava_lifecycle_regression.gd` 32 checks：检测/机动/单跳 4.0/持续致死/重生恢复/离开停止 | QA-003 同机制复测通过（2026-08-02） |

## 状态说明

- `open`：已复现，待定位或修复。
- `fixing`：Developer 正在修复。
- `self-tested`：Developer 自测通过，待独立 QA。
- `qa-passed`：独立 QA 重测通过。
- `blocked`：经过实际尝试仍受外部条件阻塞，必须附证据。

## 外部测试工具边界

- `BLOCKER-GUI-001`：Computer Use 能启动并唯一识别 fresh EXE 窗口，但激活/捕获连续失败：`failed to activate captured window`，刷新后为 `GetCursorPos failed: 拒绝访问 (0x80070005)`。已停止盲输入；改用项目 production-scene desktop InputEvent 自动化。该阻塞不等同于游戏缺陷，也不把 production-scene 证据冒充 export-EXE 人工输入。
- `BLOCKER-PERF-CI-001`：GitHub Windows Runner 的 OpenGL 3.3 不可用，Godot 自动切换到 ANGLE + `Microsoft Basic Render Driver`；该 Job 只用于验证 schema、数据一致性、缓存遥测和外部内存链路，不能作为最低/推荐玩家硬件 FPS。
- 编号约定：`BUG-REL-001` 仅表示 PowerShell 5.1 发布 smoke 兼容；`BUG-PACK-001` 仅表示 `build/*` QA 资源误入 PCK，二者不得合并。

## 当前发布阻塞仍未关闭

- `BUG-QA-COVERAGE-001`：五图连续正常路线、边界和特殊环境覆盖不足。
- `BUG-QA-CONTENT-001`：合成子领域已闭环，但其他关键领域尚未全部达到成功/失败/保存/完整重载证据。
- `BUG-QA-VISUAL-001`：最终 Windows 导出程序的全地图 E4 视觉与手感复审不足。
- `BUG-PERF-002`：最低/推荐真实 GPU 硬件与性能门槛尚未定义和实测。
